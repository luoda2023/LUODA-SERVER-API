# LUODA 服务器管理后台使用说明

## 1. 访问地址

管理后台：http://106.54.219.147:21114/_admin/
Web 客户端：http://106.54.219.147:21114/

## 2. 初始登录

用户名：admin
临时密码：Luoda@2026Admin
登录后请立即修改密码。

## 3. 客户端连接配置

```
ID 服务器：106.54.219.147
中继服务器：106.54.219.147
API 服务器：http://106.54.219.147:21114
Key：OQnLEvt6xjfPCUc1ozpTUiAxijwnn624zy0GH9IxX90=
```

## 4. 后台菜单功能

| 菜单 | 说明 |
|------|------|
| 总览 | 服务器核心状态：ID/中继/API 地址、设备数、用户数 |
| 服务器配置 | 客户端连接信息、Key、WebClient 状态 |
| 设备管理 | 设备列表：ID、主机名、用户、系统、IP、在线时间 |
| 用户管理 | 用户 CRUD：新增/编辑/改密/删除，支持管理员权限和启用状态 |
| 用户分组 | 分组管理：新增/编辑/删除 |
| 设备分组 | 设备分组管理 |
| 标签管理 | 标签 CRUD |
| 连接记录 | A/B 设备连接日志：发起端、被连接端、IP、开始/结束时间、状态 |
| 审计日志 | 连接审计、文件审计 |
| 登录令牌 | 登录 token 列表 |
| 服务器指令 | 服务器命令列表（谨慎操作） |

## 5. 用户管理详细说明

### 新增用户
点击「新增用户」按钮，填写：
- 用户名（必填，2-32 字符）
- 昵称、邮箱（可选）
- 密码（新增时必填，4-32 字符）
- 用户组 ID（默认 1）
- 管理员权限（是/否）
- 状态（启用/禁用）

### 编辑用户
点击用户行的「编辑」按钮，修改用户信息。

### 修改密码
点击「改密」按钮，输入新密码确认。

### 删除用户
点击「删除」按钮确认删除。

## 6. 连接记录

显示设备间连接日志：
- 发起端(A)：发起连接的设备名称和 ID
- 被连接端(B)：被连接的设备 ID
- IP 地址：发起端 IP
- 操作：建立/关闭
- 开始时间：连接建立时间
- 结束时间：连接关闭时间（进行中显示绿色标记）
- 会话 ID：唯一会话标识

## 7. 密钥保护

公钥：OQnLEvt6xjfPCUc1ozpTUiAxijwnn624zy0GH9IxX90=
VPS 路径：/opt/luoda-server/data/id_ed25519

禁止删除或替换密钥文件，否则已配对客户端会 KEY 不匹配。

## 8. 服务器部署

VPS：106.54.219.147
容器：luoda
目录：/opt/luoda-server

更新命令：
```bash
cd /opt/luoda-server
docker compose pull
docker compose up -d --force-recreate
```

端口：21114(API) 21115(NAT) 21116(ID) 21117(中继) 21118(WS) 21119(WebClient)

## 9. 忘记密码

```bash
docker exec luoda luoda-api reset-admin-pwd 新密码
```

## 10. GitHub 构建

仓库：https://github.com/luoda2023/LUODA-SERVER-API
推送 main 后自动构建镜像：ghcr.io/luoda2023/luoda-server:latest
